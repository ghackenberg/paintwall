// CLASSES

class ClientModel {
    constructor(clientId, name, color, width, alpha, position) {
        this.clientId = clientId
        this.name = name
        this.color = color
        this.width = width
        this.alpha = alpha
        this.position = position
    }
}