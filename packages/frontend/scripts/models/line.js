// CLASSES

class LineModel {
    constructor(lineId, clientId, color, width, alpha, points) {
        this.lineId = lineId
        this.clientId = clientId
        this.color = color
        this.width = width
        this.alpha = alpha
        this.points = points
    }
}